import React from "react";
import "./App.css";
import styled from "styled-components";

const App = () => {
  const AppContainer = styled.div`
    height: 100%;
  `;
  return <AppContainer>
    
  </AppContainer>;
};

export default App;
